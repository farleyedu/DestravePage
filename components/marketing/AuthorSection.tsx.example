import Image from "next/image";
import SectionReveal from "./SectionReveal";
import { CONFIG } from "@/lib/config";

/**
 * Seção de Autor (OPCIONAL)
 * Descomente e importe em app/page.tsx se quiser usar
 *
 * Para ativar:
 * 1. Renomeie este arquivo para AuthorSection.tsx (remova .example)
 * 2. Adicione foto em /public/images/author.jpg
 * 3. Importe em app/page.tsx:
 *    import AuthorSection from "@/components/marketing/AuthorSection";
 * 4. Adicione após a seção "O que muda na prática" (antes de Oferta)
 */

export default function AuthorSection() {
  return (
    <section className="py-section-mobile md:py-section-desktop bg-bg-base">
      <div className="container-custom">
        <SectionReveal>
          <div className="max-w-4xl mx-auto">
            <h2 className="text-h2-mobile md:text-h2-desktop font-bold text-text-primary text-center mb-12">
              Quem criou isso (e por quê)
            </h2>

            <div className="grid grid-cols-1 md:grid-cols-[200px_1fr] gap-8 items-start bg-bg-card border border-border rounded-xl p-8 md:p-12">
              {/* Foto (opcional) */}
              <div className="flex justify-center md:justify-start">
                <div className="w-40 h-40 rounded-full overflow-hidden border-4 border-border">
                  {/* Substituir por foto real */}
                  <Image
                    src="/images/author.jpg"
                    alt={CONFIG.AUTHOR.name}
                    width={160}
                    height={160}
                    className="object-cover"
                  />
                </div>
              </div>

              {/* Bio */}
              <div className="space-y-4">
                <h3 className="text-h3 font-bold text-text-primary">
                  {CONFIG.AUTHOR.name}
                </h3>

                {/* Texto editável — substituir pelo real */}
                <div className="text-body text-text-secondary space-y-4">
                  <p>
                    [Adicione aqui uma bio curta e direta — 3-4 linhas máximo.
                    Exemplo: "Passei anos sabotando minha própria execução.
                    Consumia conteúdo, sabia o que fazer, mas não fazia.
                    Este guia nasceu do que funcionou quando parei de buscar motivação e comecei a construir clareza."]
                  </p>

                  {/* Credenciais suaves (opcional) */}
                  <p className="text-sm text-text-muted italic">
                    [Opcional: adicionar credenciais discretas, sem exagero.
                    Ex: "10 anos estudando comportamento e decisão" ou
                    "Ajudou mais de 500 pessoas a sair do modo espera"]
                  </p>
                </div>
              </div>
            </div>
          </div>
        </SectionReveal>
      </div>
    </section>
  );
}
